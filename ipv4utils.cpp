#include <cstdlib>
class ipv4utils
{
        int first_byte;

      public:
        ipv4utils (int first_byte) : first_byte(first_byte)

        int[] * get_first_ip () {
                int[]* listall = malloc(sizeof(int))
        }
};